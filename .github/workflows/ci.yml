name: Github Action Demo Test

on:
  push:
    branches:
      - master


jobs:
  job1:
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - name: "Step 1"
        run: |
            echo Hello World!

      - name: "Step 2 checkout"
        uses: actions/checkout@v2

      - name: "Step 3 make"
        run: |
            make
    
    
